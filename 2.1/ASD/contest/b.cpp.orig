#include <cstdio>
using namespace std;

long long l[108];
char op[107];

long long mod = 1000000007;

long long f (

int main () {

    int n, t;

    scanf ("%d", &t);
    for (int iter = 0; iter < t; ++iter) {

        scanf ("%d", &n);
        for (int i=0; i < n; ++i) {
            scanf ("%lld", &l[i]);
        }
        scanf ("%s", &op);
        

        }


}
